# ~/.tmux.conf

# Fix modified-Enter / extended keys not working (e.g. in pi, nvim)
set -g extended-keys on
set -g extended-keys-format csi-u

# Sensible defaults
set -g mouse on
set -g history-limit 10000
set -g base-index 1
setw -g pane-base-index 1
